name: Acceptance Tests

on:
  push:
    branches:
      - feat/pocket-id-2.0.2-compatibility

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: '1.20'
      - run: make test-acc-local
